import styled from 'styled-components';

export const Header = styled.h1`
   margin-top: 50px;
   font-weight: 600;
   font-size: 24px;
   font-family: segoe;
   color: rgba(0,0,0,0.85);
   text-align: center;
`